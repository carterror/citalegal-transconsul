version: 1.0
runtime: python311
build:
  commands:
    build:
      - pip3 install pipenv
      - pipenv install -r requirements.txt
run:
  runtime-version: 3.11
  command: pipenv run gunicorn configs.wsgi --log-file -
  network:
    port: 8000